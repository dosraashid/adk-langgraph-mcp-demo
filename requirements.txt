# --- AI Inference ---
langchain-core>=1.0.0          # Essential core logic for message handling
gradient                       # Official SDK to access GPT-5.4 on DigitalOcean

# --- Orchestration & Memory ---
langgraph>=0.2.0               # Manages reasoning loops and persistent state

# --- Model Context Protocol (MCP) ---
mcp>=1.2.0                     # The core official Python SDK for MCP
langchain-mcp-adapters>=0.2.0  # Required for modern tool-loading logic

# --- Utilities ---
gradient-adk                   # Allows running via 'gradient agent run'
python-dotenv                  # Securely loads your .env variables
